# No Frontmatter Skill

This SKILL.md has no YAML frontmatter. It should be rejected during source registration.

The absence of `---` delimiters and `name`/`description` fields makes this invalid
per the Agent Skills specification.
